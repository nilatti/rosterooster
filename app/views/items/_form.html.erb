<%= form_for [@event, @item] do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :time_needed %><br>
    <%= f.datetime_select :time_needed %>
  </div>
  <div class="field">
    <%= f.label :location_needed %><br>
    <%= f.text_field :location_needed %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
