<%= form_for(@promise) do |f| %>
  <% if @promise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@promise.errors.count, "error") %> prohibited this promise from being saved:</h2>

      <ul>
      <% @promise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :person_id %><br>
    <%= f.number_field :person_id %>
  </div>
  <div class="field">
    <%= f.label :registration_id %><br>
    <%= f.number_field :registration_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
